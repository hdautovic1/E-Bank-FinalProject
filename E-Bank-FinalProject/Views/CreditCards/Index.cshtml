@model IEnumerable<E_Bank_FinalProject.Models.CreditCard>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CreditCardName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Account)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CreditCardName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Account.AccountName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.CreditCardID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.CreditCardID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.CreditCardID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
